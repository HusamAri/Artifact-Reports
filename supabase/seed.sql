-- Local development seed. Safe to run repeatedly.
-- Skipped in production by Supabase CLI when --no-seed is set.

-- Example workspace (requires a profile to exist via auth.users first).
-- Intentionally minimal — full seed lands in M2.
